using System;
using System.IO;
using System.Net;
using System.Net.Sockets;

namespace AvaloniaReactorUI.HotReloadConsole
{
//     class Program
//     {
//         static void Main(string[] args)
//         {
// #if DEBUG
//             if (args == null ||
//                 args.Length == 0)
//             {
//                 args = new[] { @"AvaloniaReactorUI.DemoApp.dll" };
//             }
// #endif
//             if (args == null ||
//                 args.Length == 0)
//                 return;

//             using var tcpClient = new TcpClient();
//             tcpClient.Connect(IPAddress.Loopback, 45821);

//             using var sw = new StreamWriter(tcpClient.GetStream());
//             sw.WriteLine($"RELOAD|{args[0]}");
//         }
//     }
}
